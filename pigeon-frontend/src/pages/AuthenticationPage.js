import React from 'react';

const AuthenticationPage = () => {
    
    return (
        <div>
            this is a page for authentication. the component rendering this is AuthenticationPage
        </div>
    );
}

export default AuthenticationPage;